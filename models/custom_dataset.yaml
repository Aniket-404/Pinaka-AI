# Custom YOLO Dataset Configuration
# Dataset root directory (relative path for portability)
path: .

# Training and validation sets (relative to path)
train: custom_dataset/images/train
val: custom_dataset/images/val

# Number of classes
nc: 2

# Class names
names:
  0: stone
  1: gas_cylinder
